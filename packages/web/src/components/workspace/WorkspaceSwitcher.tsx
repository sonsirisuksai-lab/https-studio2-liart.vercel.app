import React from 'react';

export const WorkspaceSwitcher = () => null;
export const WorkspaceLayout = ({ children }: { children: React.ReactNode }) => <>{children}</>;
export default WorkspaceLayout;
